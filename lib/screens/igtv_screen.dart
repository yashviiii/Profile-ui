import 'package:flutter/material.dart';

class Igtv extends StatelessWidget {
  const Igtv({required Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
